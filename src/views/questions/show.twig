{% extends "layouts/_default.twig" %}

{% block main_content %}
    <div>
        <h1>{{ question.question }}</h1>
        <p><strong>ID: </strong>{{ question.id }}</p>
        <p><strong>Quiz ID: </strong>{{ question.QuizId }}</p>
        <p><strong>Quiz Title: </strong>{{ question.Quiz.name }}</p>
        <p><strong>Created At: </strong>{{ question.createdAt }}</p>
        <p><strong>Updated Last: </strong>{{ question.updatedAt }}</p>
        <p>
            <a href="/questions/{{ question.id }}/delete">Delete</a>
        </p>
        <p>
            <a href="/questions">Back to Questions</a>
        </p>
    </div>
{% endblock %}